import { DefaultTheme } from "react-native-paper";

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    text: "#000000",
    orange: "#EA3C12",
    gray: "#8d8d8d",
    error: "#f13a59",
    white: "#fff",
  },
};
